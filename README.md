Clone the repository and run:

`npm install`

Then start the local server:

`hugo server --config config.toml --baseURL=http://localhost:1313`

or

`hugo server --config config-2.toml --baseURL=http://localhost:1313`


Open http://localhost:1313/ in your browser to view the website running locally.
